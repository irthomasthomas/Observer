# Partner Dashboard

Placeholder for the Observer AI partner dashboard.

This will be a Vite + React + Tailwind app, matching the setup of the sibling
sites in `websites/` (`landing/`, `status/`).

## Cloudflare Pages config (when ready)

- **Build command**: `cd websites/partner_dashboard && npm install && npm run build`
- **Build output directory**: `websites/partner_dashboard/dist`
- **Root directory**: `/` (or leave empty)
